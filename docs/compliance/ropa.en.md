# Record of processing activities (RoPA) — appgrove

> **INTERNAL** document (art. 30(4) GDPR): provided to the supervisory authority on request only; this is not the public privacy policy (#13 C17). **GENERATED file** from the data manifests (`docs/compliance/manifests/*.yaml`) via `tools/compliance` — **do not edit by hand**: update the manifest and regenerate (`npm run assemble`). Draft under disclaimer: final validation by legal counsel (docs/_REVISIONE-LEGALE.md).

## appgrove platform (core)

Cross-cutting platform processing (accounts, person identities, memberships, invitations, authentication, logs). After UC 0116 the person's identity is **platform** data (email, name, language, authentication identifier) and account membership is **account** data: the purpose does not change, the party accountable for the data does. appgrove acts as controller towards B2C consumers and as processor for B2B tenant controllers (#13 C13). Paddle (Merchant of Record) is an independent controller for payment data.

### Processing activities

| Entry | Data category | Location | Data subjects | Purpose | Legal basis | Retention |
|---|---|---|---|---|---|---|
| `identity.email` | Contact data (email address) | Table `platform.identity` (Aurora PostgreSQL, eu-west-1) — platform data, not account data | People registered on the platform (one identity, one or more account memberships) | Account provisioning and management (login, service communications) | Contract (art. 6(1)(b) GDPR) | While the person has at least one active membership + 14-day grace period after the last account is deleted (#13 E25); deleting one account does not erase the identity of someone who also belongs to other accounts (UC 0116) |
| `identity.display_name` | Identity (display name) | Table `platform.identity` (Aurora PostgreSQL, eu-west-1) — platform data, not account data | People registered on the platform | Identification of the person in the UI and across the accounts they belong to | Contract (art. 6(1)(b) GDPR) | While the account is active + 14-day grace period after deletion (#13 E25) |
| `identity.locale` | User preference (language) | Table `platform.identity` (Aurora PostgreSQL, eu-west-1) — platform data, not account data | People registered on the platform | Language selection (English/Italian) for transactional authentication emails (address verification, password reset, invitation) — UC 0018 | Contract (art. 6(1)(b) GDPR) — transactional emails, distinct from the newsletter (separate consent, | While the account is active + 14-day grace period after deletion (#13 E25) |
| `identity.cognito_sub` | Online identifier (Cognito subject, pseudo-identifier) | Table `platform.identity` (Aurora PostgreSQL, eu-west-1) — platform data, not account data | People registered on the platform | Link between the authentication identity (Cognito) and the application profile | Contract (art. 6(1)(b) GDPR) | While the account is active + 14-day grace period after deletion (#13 E25) |
| `identity.active_membership_id` | Technical session preference (reference to the membership the person is working in): it adds no link between person and company, which is already declared on the membership | Table `platform.identity` (Aurora PostgreSQL, eu-west-1) — platform data, not account data | People registered on the platform | Remembering which account the person was working in, so that with several memberships the session knows on whose behalf it acts (UC 0117). The value is a hint: the membership is re-verified on every token creation | Contract (art. 6(1)(b) GDPR) | While the person has at least one active membership (+ 14-day grace period, |
| `membership.identity_id` | Online identifier (reference to the person's identity): the link between a person and a company is itself personal data, even though it contains neither name nor email | Table `platform.membership` (Aurora PostgreSQL, eu-west-1) — account data | People belonging to an account (members, administrators, account owners) | Establishing who may act as a person of that account and in which role (UC 0116); the account remains the data boundary. From UC 0103 the NUMBER of live memberships, plus pending invitations, also determines the amount the account owes for its seats: this is a clarification of purpose within the already declared billing processing — no new category, no new legal basis, no new recipient (the payment provider receives the amount and the account identifier, never the list of people) | Contract (art. 6(1)(b) GDPR) | While the membership is live; deleted with the account (+ 14-day grace period, #13 E25). Leaving an account closes the membership and does not touch the person's identity |
| `app_access.identity_id` | Authorisation data: online identifier (reference to the person's identity) paired with the role on a single application. It contains neither name nor email, but it states which application that person uses and with which power | Table `platform.app_access` (Aurora PostgreSQL, eu-west-1) — account data | People of the account enabled to use an application | Allowing the people of the account to use the purchased applications with the role assigned by the owner or by that application's administrator (UC 0098) | Contract with the controlling account (art. 6(1)(b) GDPR) | Tied to the life of the membership: access is deleted when the person leaves the account and with the account itself (+ 14-day grace period, #13 E25). Revocation is a logical deletion, so that re-granting access does not require rebuilding its history |
| `invitations.email` | Contact data (invitee email address) | Table `platform.invitations` (Aurora PostgreSQL, eu-west-1) | People invited to join a tenant (not yet users) | Delivery and management of the invitation (single-use, expiring; the token is stored hashed only) | Pre-contractual measures / contract (art. 6(1)(b) GDPR) | Until the invitation expires or is accepted; soft-delete and purge per the platform lifecycle |
| `invitations.identity_id` | Online identifier (reference to the invited person's identity), set only when that address already has an identity on the platform | Table `platform.invitations` (Aurora PostgreSQL, eu-west-1) — account data | People invited to join an account who already have an appgrove identity | Linking the invitation to a person who already exists, so they join with one more membership instead of a second identity (UC 0118) | Pre-contractual measures / contract (art. 6(1)(b) GDPR) | Until the invitation expires or is closed; deleted with the account (+ 14-day grace period, #13 E25). It is NEVER returned to the inviter, and is exported only for ACCEPTED invitations — on a pending invitation it would reveal that the person already has a relationship with the platform, which is not the inviting account's information (UC 0118 §5) |
| `invitations.seat_charge_ref` | Billing data: opaque reference to the payment-provider transaction that authorised that person's seat. It contains neither amounts nor payment details, and is declared out of caution: it sits on a row whose subject is a person, and links that person to a payment | Table `platform.invitations` (Aurora PostgreSQL, eu-west-1) — account data | People invited to join an account whose seat was paid for | Being able to show that the person's seat was paid before the invitation went out (UC 0103), and to reverse the charge if the invitation is not created | Contract (art. 6(1)(b) GDPR) — performance of the contract with the account holder | Same as the invitation carrying it: until the invitation expires or is closed; deleted with the account (+ 14-day grace period, #13 E25). Null when the seat cost nothing (free allowance, or a seat already paid for in the current period) |
| `newsletter_subscribers.email` | Contact data (subscriber email address) | Table `platform.newsletter_subscriber` (Aurora PostgreSQL, eu-west-1) | Newsletter subscribers (site visitors and registered users) | Newsletter delivery (direct marketing), with double opt-in and proof of consent in the consent log (art. 7); Plausible remains aggregate, non-identifying analytics | Consent (art. 6(1)(a) GDPR) — separate and distinct from transactional emails (#13 F29) | While subscribed + 24 months after unsubscription, then automatic deletion (#13 E) |
| `accounts.name` | Identity / account name (prudent classification — may be a company name) | Table `platform.accounts` (Aurora PostgreSQL, eu-west-1) | Account holders (for individual B2C accounts it is typically the person's name) | Identification of the account/tenant across the platform and communications | Contract (art. 6(1)(b) GDPR) | While the account is active + 14-day grace period after deletion (#13 E25) |
| `accounts.paddle_customer_id` | Online identifier (customer id at Paddle, pseudo-identifier) | Table `platform.accounts` (Aurora PostgreSQL, eu-west-1) | Account holders with a subscription | Subscription/payment reconciliation with Paddle (Merchant of Record, independent controller — #13 H) | Contract (art. 6(1)(b) GDPR) | While the account is active + 14-day grace period; fiscal retention on payments rests with Paddle |
| `cognito.credentials` | Authentication credentials (password hash) and MFA/TOTP secrets | Amazon Cognito user pool (eu-west-1); locally a dev-only provider (`services/auth`, out of RoPA scope) | People registered on the platform | User authentication and access protection (MFA) | Contract (art. 6(1)(b) GDPR) | While the account is active; removed when the Cognito user is deleted |
| `support_ticket.subject` | Free-form content (subject of the support/privacy request). Being user-written it may contain, on data-rights requests, references to special categories (art. 9): the text is never profiled, but a deterministic keyword screening flags the request for human attention and raises its priority (UC 0075). The flag is an operational boolean: it does NOT record which category was recognised. | Table `platform.support_ticket` (Aurora PostgreSQL, eu-west-1) | Users opening support or data-rights requests | Handling support and data-rights requests (in-house ticketing, #13 D21) | Contract (art. 6(1)(b) GDPR); for privacy tickets also legal obligation (art. 12 GDPR) | 24 months from ticket closure, then automatic deletion (#13 E) |
| `support_ticket_message.body` | Free-form content (support-thread message text). Same note as the subject: user-written text may contain special categories (art. 9), with an operational flag for human attention and no category recorded (UC 0075). Thread content never travels by email: notifications link back to the page. | Table `platform.support_ticket_message` (Aurora PostgreSQL, eu-west-1) | Users writing in a support-ticket thread | Handling support and data-rights requests (in-house ticketing, #13 D21) | Contract (art. 6(1)(b) GDPR); for privacy tickets also legal obligation (art. 12 GDPR) | 24 months from ticket closure, then automatic deletion (#13 E) |
| `legal_acceptances.user_id` | Online identifier (Cognito subject) linked to the acceptance record | Table `platform.legal_acceptance` (Aurora PostgreSQL, eu-west-1) | Registered users accepting or acknowledging the legal documents | Proof of acceptance/acknowledgement of the legal documents (accountability, art. 5(2) GDPR; UC 0056) | Contract (art. 6(1)(b) GDPR) and accountability obligation (art. 5(2) GDPR) | For the account lifetime and the applicable limitation period (proof of contractual consent, #13 E) — to be confirmed in legal review |
| `billing_transactions.paddle_transaction_id` | Online identifier (transaction id at Paddle, pseudo-identifier) | Table `platform.billing_transaction` (Aurora PostgreSQL, eu-west-1) | Account holders who made a payment (for individual B2C accounts, natural persons) | Payment history and receipts shown to the account holder on the Billing page (UC 0096) and reconciliation with Paddle (Merchant of Record, independent controller — #13 H) | Contract (art. 6(1)(b) GDPR) | While the account is active + 14-day grace period; fiscal retention on payments rests with Paddle |
| `billing_transactions.amount` | Billing data (transaction amount and currency). NOT payment instrument data: no card or bank details ever transit through or are stored by appgrove (PCI scope rests with Paddle) | Table `platform.billing_transaction` (Aurora PostgreSQL, eu-west-1) | Account holders who made a payment (for individual B2C accounts, natural persons) | Showing the account holder what was paid and when (contract performance, UC 0096) | Contract (art. 6(1)(b) GDPR) | While the account is active + 14-day grace period; fiscal retention on payments rests with Paddle |
| `billing_transactions.fee_amount` | Billing data (fee withheld by Paddle on the transaction). Like the amount, NOT payment instrument data: no card or bank details ever transit through or are stored by appgrove | Table `platform.billing_transaction` (Aurora PostgreSQL, eu-west-1) | Account holders who made a payment (for individual B2C accounts, natural persons) | Reconciling gross revenue with the money actually paid out (platform economic observability, UC 0071). Not shown to the customer: the view is restricted to the platform administrator | Contract (art. 6(1)(b) GDPR) | While the account is active + 14-day grace period; fiscal retention on payments rests with Paddle |
| `billing_transactions.net_amount` | Billing data (net amount remaining after the provider fee) | Table `platform.billing_transaction` (Aurora PostgreSQL, eu-west-1) | Account holders who made a payment (for individual B2C accounts, natural persons) | Reconciling gross revenue with the money actually paid out (platform economic observability, UC 0071). Not shown to the customer | Contract (art. 6(1)(b) GDPR) | While the account is active + 14-day grace period; fiscal retention on payments rests with Paddle |
| `billing_transactions.receipt_url` | Link to a payment document hosted by Paddle (receipt made out to the customer) | Table `platform.billing_transaction` (Aurora PostgreSQL, eu-west-1) — the document itself is hosted by Paddle | Account holders who made a payment | Letting the account holder open the receipt for their own payment (UC 0096) | Contract (art. 6(1)(b) GDPR) | While the account is active + 14-day grace period; the document is retained by Paddle (MoR) |
| `logs.structured` | Technical identifiers in structured logs (`tenant_id`, `app_id`, `user_id`, IP) | CloudWatch Logs (eu-west-1/eu-central-1); audit trail on S3/Glacier (#08 I) | Platform users | Security, stability and diagnostics (purpose limitation — no profiling, #13 B12) | Legitimate interest (art. 6(1)(f) GDPR — service security and continuity) | Prod application logs 30 days; audit/security 12 months on S3→Glacier (#08 I26) |
| `logs.frontend_errors` | JavaScript ERROR-only events (#08 H23): message/stack, route, build version, opaque `user_id`/`tenant_id` when a session exists. No IP, no user agent, no behavioural tracking. | CloudWatch Logs (eu-west-1), error-ingest log group (`/aws/lambda/appgrove-<env>-error-ingest`) | SPA users (including unauthenticated) | Frontend diagnostics and stability (purpose limitation — errors only, #13 B12) | Legitimate interest (art. 6(1)(f) GDPR — service quality and continuity) | Same as application logs (test 7 days, prod 30 days, |

### Recipients and sub-processors

Sub-processors: **AWS** (hosting, EU regions — DPA with SCCs + DPF certification) and **Plausible Analytics** (cookieless analytics, EU hosting). **Paddle** (Merchant of Record) is an **independent controller** for payment data, not a sub-processor (#13 H45-47). Public list: `content/legal/subprocessors.<lang>.md` (UC 0002).

### Non-EU transfers

Data at rest only in EU regions (eu-west-1; monitoring in eu-central-1) — #13 I51. AWS Inc. (US, CLOUD Act): DPF + SCC safeguards in the DPA, plus at-rest/in-transit encryption (#13 I52).

### Security measures

At-rest and in-transit encryption; row-level per-tenant isolation (`tenant_id` only from the verified JWT); least-privilege IAM; invitation tokens stored hashed only; soft-delete with scheduled purge (14-day grace); structured logging and audit trail (#02/#05/#06/#08).

### Personal data breach response

Incident Response process prepared in advance (the 72 hours run from awareness): internal runbook `docs/compliance/breach-runbook.md` (detect → assess → contain → notify → document) with a risk-to-data-subjects threshold tree and the encryption lever (art. 34(3)); internal breach register `docs/compliance/breach-register.md` recording ALL breaches, including non-notified ones (art. 33(5)); role-based notification (controller → Garante within 72h art. 33 / data subjects art. 34; processor → the tenant-controller without delay); responsible-disclosure channel `security@appgrove.app` + `security.txt` (#13 J56–J64).

## Mini-CRM app (multi-user B2B contact management)

Contact data (people at the tenant's client organizations) entered by the tenant into their CRM (schema `app_crm`). The tenant is the data controller; appgrove acts as processor (#13 A2/C13). The `seat` table holds only the internal identifier of the tenant's members granted access to the app — processed by core as controller — and is therefore not third-party data. The same holds for the two **local copies** in the schema (`entitlement_projection`, `app_role_projection`): they hold the account identifier, the person's internal authentication identifier and their role on the app — no name, no address. They are copies of data controlled by core (`platform.app_access`, UC 0098/0099), exist only to avoid calling core on every request, and are physically deleted with the account upon erasure (the count is recorded in the purge audit trail).

### Processing activities

| Entry | Data category | Location | Data subjects | Purpose | Legal basis | Retention |
|---|---|---|---|---|---|---|
| `contact.display_name` | Contact identity (name) | Table `app_crm.contact` (Aurora PostgreSQL, eu-west-1) | Contacts (people at the tenant's client organizations) entered by the tenant | Managing the tenant's commercial relationship with its contacts | Contract (art. 6(1)(b) GDPR) | Until deletion by the tenant or account closure |
| `contact.email` | Contact details (email address, optional) | Table `app_crm.contact` (Aurora PostgreSQL, eu-west-1) | Contacts (people at the tenant's client organizations) entered by the tenant | Managing the tenant's commercial relationship with its contacts | Contract (art. 6(1)(b) GDPR) | Until deletion by the tenant or account closure |
| `contact.phone` | Contact details (phone number, optional) | Table `app_crm.contact` (Aurora PostgreSQL, eu-west-1) | Contacts (people at the tenant's client organizations) entered by the tenant | Managing the tenant's commercial relationship with its contacts | Contract (art. 6(1)(b) GDPR) | Until deletion by the tenant or account closure |
| `contact.notes` | Free-text notes about the contact. Unstructured field: may contain unclassified information, including — at the tenant's initiative — special categories (art. 9). The tenant controller's privacy notice must cover it; appgrove does not solicit them. | Table `app_crm.contact` (Aurora PostgreSQL, eu-west-1) | Contacts entered by the tenant, and any third parties mentioned in the notes | Managing the tenant's commercial relationship with its contacts | Contract (art. 6(1)(b) GDPR) | Until deletion by the tenant or account closure |
| `interaction.note` | Free-text content of an interaction (call, email, meeting, note). Like the contact notes, it is an unstructured field and a possible unguarded entry point for unclassified data. | Table `app_crm.interaction` (Aurora PostgreSQL, eu-west-1) | Contacts entered by the tenant, and any third parties mentioned in the interactions | History of the tenant's commercial relationship with its contacts | Contract (art. 6(1)(b) GDPR) | Until deletion by the tenant or account closure |

## Fatture app (single-user B2C invoicing)

End-customer data entered by the tenant in their invoices (schema `app_fatture`). The tenant is the data controller; appgrove acts as processor (#13 C13). The two **local copies** in the schema (`entitlement_projection`, `app_role_projection`) hold the account identifier, the person's internal authentication identifier and their role on the app — no name, no address: they are copies of data controlled by core (`platform.app_access`, UC 0098/0099), exist only to avoid calling core on every request, and are physically deleted with the account upon erasure.

### Processing activities

| Entry | Data category | Location | Data subjects | Purpose | Legal basis | Retention |
|---|---|---|---|---|---|---|
| `invoice.customer_name` | Customer identity (name) | Table `app_fatture.invoice` (Aurora PostgreSQL, eu-west-1) | Customers (invoice recipients) of the tenant | Invoice issuance and management | Contract (art. 6(1)(b) GDPR) and the controller's legal obligations (art. 6(1)(c)) | 10 years from issuance (fiscal obligations) |
| `invoice.customer_email` | Customer contact data (email address, optional) | Table `app_fatture.invoice` (Aurora PostgreSQL, eu-west-1) | Customers (invoice recipients) of the tenant | Invoice delivery | Contract (art. 6(1)(b) GDPR) | 10 years from issuance (fiscal obligations) |

