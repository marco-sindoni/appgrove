<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>app.appgrove</groupId>
    <artifactId>appgrove-services</artifactId>
    <version>0.1.0</version>
  </parent>

  <artifactId>core</artifactId>
  <name>appgrove :: services :: core</name>
  <description>Core/platform service Quarkus (schema platform). Le entità di dominio arrivano in UC 0013.</description>

  <properties>
    <!-- AWS SDK v2 per il client SQS verso ElasticMQ (stub Paddle, UC 0023). UC 0025 valuterà
         il passaggio all'estensione Quarkus amazon-sqs per il deploy cloud. -->
    <awssdk.version>2.31.78</awssdk.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>software.amazon.awssdk</groupId>
        <artifactId>bom</artifactId>
        <version>${awssdk.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>app.appgrove</groupId>
      <artifactId>commons</artifactId>
      <version>${project.version}</version>
    </dependency>

    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest-jackson</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-hibernate-orm-panache</artifactId>
    </dependency>
    <!-- Pricing-as-code (UC 0022): parsing YAML della definizione di catalogo (versione dal BOM Quarkus). -->
    <dependency>
      <groupId>com.fasterxml.jackson.dataformat</groupId>
      <artifactId>jackson-dataformat-yaml</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-jdbc-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-flyway</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-smallrye-jwt</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-hibernate-validator</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-smallrye-openapi</artifactId>
    </dependency>

    <!-- Osservabilità di base (UC 0006/#08): log JSON, tracing strumentato (export spento),
         metriche Micrometer→EMF (bridge in commons), health liveness/readiness. -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-logging-json</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-opentelemetry</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-micrometer</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-smallrye-health</artifactId>
    </dependency>
    <!-- Notifiche email del ticketing (UC 0034, #13 D21): Mailpit in dev, MockMailbox in test;
         il relay SES nel cloud è infrastruttura tracciata (UC 0034 "Punti aperti"). -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-mailer</artifactId>
    </dependency>

    <!-- Stub Paddle (UC 0023): poller consumer schedulato + client SQS verso ElasticMQ -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-scheduler</artifactId>
    </dependency>
    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>sqs</artifactId>
      <exclusions>
        <!-- usiamo il client HTTP sync url-connection (leggero), non Netty/Apache -->
        <exclusion>
          <groupId>software.amazon.awssdk</groupId>
          <artifactId>netty-nio-client</artifactId>
        </exclusion>
        <exclusion>
          <groupId>software.amazon.awssdk</groupId>
          <artifactId>apache-client</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>url-connection-client</artifactId>
    </dependency>

    <!-- Test -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-junit5</artifactId>
      <scope>test</scope>
    </dependency>
    <!--
      Mock CDI nei collaudi (UC 0103): serve a provare il percorso di guasto più pericoloso della storia
      dei posti — l'addebito riesce e la creazione dell'invito fallisce subito dopo, quindi l'addebito va
      annullato. Quel percorso non si raggiunge da fuori: nessun dato di ingresso lo provoca, perché è per
      definizione un guasto. Provarlo richiede di farlo accadere, e l'alternativa — non provarlo — lascerebbe
      senza rete l'unico ramo in cui un cliente può pagare per qualcosa che non ha.
    -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-junit5-mockito</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.rest-assured</groupId>
      <artifactId>rest-assured</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.smallrye</groupId>
      <artifactId>smallrye-jwt-build</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit</artifactId>
      <version>${archunit.version}</version>
      <scope>test</scope>
    </dependency>
    <!-- Verifier @PersonalData ↔ manifesto dati (UC 0030) dal test-jar di commons. -->
    <dependency>
      <groupId>app.appgrove</groupId>
      <artifactId>commons</artifactId>
      <version>${project.version}</version>
      <type>test-jar</type>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!--
        Template dell'email di conferma newsletter (UC 0039): la SORGENTE UNICA è
        shared/email-templates alla radice del repo (la stessa delle email auth). Qui viene
        copiata nell'artefatto invece di essere duplicata — due copie del copy divergerebbero
        in silenzio. Stesso passo di services/auth/pom.xml.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <executions>
          <execution>
            <id>copy-email-templates</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.outputDirectory}/email-templates</outputDirectory>
              <resources>
                <resource>
                  <directory>${project.basedir}/../../shared/email-templates</directory>
                  <includes>
                    <include>*.json</include>
                    <include>layout.*</include>
                  </includes>
                </resource>
              </resources>
            </configuration>
          </execution>
          <!--
            Documenti legali (UC 0056): la SORGENTE UNICA è content/legal/ alla radice del repo (la
            stessa che rende il sito). Qui i .md (5 lingue) + entity.yaml vengono bundlati in classpath
            (/legal) invece di essere duplicati: la sync popola legal_version dai frontmatter e
            l'endpoint /legal/{component} rende il testo coi token {{titolare.*}} risolti in-app.
          -->
          <execution>
            <id>copy-legal-content</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.outputDirectory}/legal</outputDirectory>
              <resources>
                <resource>
                  <directory>${project.basedir}/../../content/legal</directory>
                  <includes>
                    <include>*.md</include>
                    <include>entity.yaml</include>
                  </includes>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>${quarkus.platform.group-id}</groupId>
        <artifactId>quarkus-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
