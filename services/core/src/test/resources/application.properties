# ── Config di TEST (UC 0013) ──────────────────────────────────────────────────
# Postgres reale via Quarkus Dev Services (Testcontainers), immagine 17.
quarkus.datasource.db-kind=postgresql
quarkus.datasource.devservices.enabled=true
quarkus.datasource.devservices.image-name=postgres:17

# Flyway reale: migrazioni di produzione su schema platform (V1 schema + V2 modello dominio).
quarkus.flyway.migrate-at-start=true
quarkus.flyway.schemas=platform
quarkus.hibernate-orm.database.generation=none
quarkus.hibernate-orm.multitenant=DISCRIMINATOR

# JWT firmati localmente dall'harness di test (smallrye-jwt) — nessuna dipendenza da Cognito.
mp.jwt.verify.publickey.location=publicKey.pem
mp.jwt.verify.issuer=https://local.appgrove.app
smallrye.jwt.sign.key.location=privateKey.pem

# Client entitlement (commons): URL richiesto dalla config; core non lo usa (espone /me/entitlements).
quarkus.rest-client.core-api.url=http://localhost:8080

# ── Stub Paddle (UC 0023) in test ─────────────────────────────────────────────
# Provider stub (default), endpoint dev attivi per testare il trigger scenari + read subscription.
appgrove.payments.dev-endpoints=true
# La coda SQS reale è sostituita da una InMemoryWebhookQueue (@io.quarkus.test.Mock): niente ElasticMQ/
# LocalStack nei test (deterministici, offline). Il consumer è guidato esplicitamente via drain().
quarkus.scheduler.enabled=false

# Pricing-as-code (UC 0022): sync allo startup anche in test → il catalogo (app/tier/price) è popolato
# dagli YAML prima che SeedDataTest applichi seed.sql (le subscription del seed referenziano il catalogo).
appgrove.pricing.sync-on-startup=true
# Carica le app fixture (notes/teams/legacy) in test: SeedDataTest/AdminApiTest asseriscono su quel catalogo.
appgrove.pricing.include-fixtures=true

# ── Framework GDPR (UC 0032) in test ──────────────────────────────────────────
# Code e storage sostituiti da mock in-memory (@Mock); worker/consumer guidati via drain().
appgrove.gdpr.audit-table=platform.gdpr_purge_audit

# ── Osservabilità (UC 0006) in test ───────────────────────────────────────────
# Tracing strumentato ma export spento anche in test (deterministico/offline: nessun tentativo
# OTLP verso localhost). Override espliciti del profilo principale (le application.properties di
# main/test si SOMMANO, quella di test vince): log console in testo leggibile — la forma JSON è
# verificata da JsonLogLayoutTest — e bridge EMF inerte (niente publisher su stdout nei test).
quarkus.otel.traces.exporter=none
quarkus.otel.metrics.exporter=none
quarkus.otel.logs.exporter=none
quarkus.log.console.json=false
appgrove.metrics.emf.enabled=false
# Il probe MDC di test NON deve finire nello spec OpenAPI committato (store-schema gira anche
# nell'augmentation di test): questo override RIMPIAZZA il valore di main → va ripetuto anche
# PaddleStubDevResource.
mp.openapi.scan.exclude.classes=app.appgrove.core.billing.PaddleStubDevResource,app.appgrove.core.observability.MdcProbeResource
