package app.appgrove.core.catalog;

/** Modello utente di un'app: {@code single_user} (B2C) o {@code multi_user} (B2B, con membership). */
public enum AppUserModel {
    single_user,
    multi_user
}
