package app.appgrove.core.support;

/** Autore di un messaggio nel thread: utente del tenant, platform-admin o il sistema (auto-ticket). */
public enum TicketAuthor {
    user,
    admin,
    system
}
